Tim Backend Have used the fastapi to create the endpoint route for the app.
the bakend directory has the app.py file that controls the routing and apis
Then the predict.py controls the functions for the ML
I'm managing the environment using UV - wrapper .
